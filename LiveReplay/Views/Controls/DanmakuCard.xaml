<UserControl x:Class="LiveReplay.Views.Controls.DanmakuCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="300"
             Opacity="0" RenderTransformOrigin="0.5,0.5"
             HorizontalAlignment="Left">
    <UserControl.RenderTransform>
        <TranslateTransform Y="-10"/>
    </UserControl.RenderTransform>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.2">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid Margin="50,6,10,6" HorizontalAlignment="Left">
        <!-- 气泡主体 - 宽度跟随内容自适应 -->
        <Border Background="#F5F3F2" CornerRadius="4,10,4,10" Padding="10,6,14,4"
                HorizontalAlignment="Left">
            <Border.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.3" Color="#2D2867"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧内容 -->
                <StackPanel Grid.Column="0">
                    <!-- 用户名标签 -->
                    <Border x:Name="UserNameBorder" CornerRadius="4" Padding="4,2" Margin="0,0,0,3"
                            Background="#666666" HorizontalAlignment="Left">
                        <TextBlock x:Name="UserNameText" Text="{Binding UserName}"
                                   FontSize="10" Foreground="White" FontWeight="Medium"/>
                    </Border>

                    <!-- 弹幕内容 - MaxWidth由代码动态设置 -->
                    <TextBlock x:Name="ContentText" Text="{Binding Content}"
                               FontSize="13" Foreground="#333333" TextWrapping="Wrap"
                               MaxHeight="48" TextTrimming="CharacterEllipsis"
                               LineHeight="17"/>
                </StackPanel>

                <!-- 右侧装饰条 -->
                <Border Grid.Column="1" Width="6" CornerRadius="0,10,10,0"
                        Background="#666666" Margin="8,0,-14,-4"
                        VerticalAlignment="Stretch"/>
            </Grid>
        </Border>

        <!-- 头像（独立显示在左侧） -->
        <Border Width="32" Height="32" CornerRadius="16"
                Background="#E0E0E0" Margin="-46,2,0,0"
                HorizontalAlignment="Left" VerticalAlignment="Top">
            <Border.Effect>
                <DropShadowEffect BlurRadius="2" ShadowDepth="0" Opacity="0.3" Color="#2D2867"/>
            </Border.Effect>
            <Border.Clip>
                <EllipseGeometry Center="16,16" RadiusX="16" RadiusY="16"/>
            </Border.Clip>
            <Image x:Name="AvatarImage" Stretch="UniformToFill"/>
        </Border>
    </Grid>
</UserControl>