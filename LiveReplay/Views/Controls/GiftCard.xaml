<UserControl x:Class="LiveReplay.Views.Controls.GiftCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="280"
             Opacity="0" RenderTransformOrigin="0.5,0.5">
    <UserControl.RenderTransform>
        <TranslateTransform Y="-15"/>
    </UserControl.RenderTransform>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-15" To="0" Duration="0:0:0.25">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Border x:Name="CardBorder" CornerRadius="8" Margin="6,3" Padding="10,6">
        <Border.Background>
            <SolidColorBrush Color="#E0F4FF" Opacity="0.5"/>
        </Border.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 圆形头像 -->
            <Border Grid.Column="0" Width="32" Height="32" CornerRadius="16"
                    Background="#3A3A3A" Margin="0,0,10,0">
                <Border.Clip>
                    <EllipseGeometry Center="16,16" RadiusX="16" RadiusY="16"/>
                </Border.Clip>
                <Image x:Name="AvatarImage" Stretch="UniformToFill"/>
            </Border>

            <!-- ID -->
            <TextBlock x:Name="UserNameText" Grid.Column="1" Text="{Binding UserName}"
                       FontSize="12" Foreground="#555" VerticalAlignment="Center"
                       Margin="0,0,8,0"/>

            <!-- 礼物名称和数量 -->
            <TextBlock x:Name="GiftText" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,8,0">
                <Run Text="{Binding GiftName}" Foreground="#555" FontSize="12"/>
                <Run Text=" ×" Foreground="#555" FontSize="12"/>
                <Run Text="{Binding GiftCount}" Foreground="#FFD700" FontSize="12" FontWeight="Bold"/>
            </TextBlock>

            <!-- 价格 (一位小数) -->
            <TextBlock x:Name="PriceText" Grid.Column="3" VerticalAlignment="Center">
                <Run Text="￥" Foreground="#FFD700" FontSize="11"/>
                <Run Text="{Binding Price, StringFormat={}{0:F1}}" Foreground="#FFD700" FontSize="13" FontWeight="Bold"/>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>